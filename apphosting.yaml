# apphosting.yaml

# Define o serviço de backend
services:
- id: backend
  root_directory: backend
  
# As rotas para a sua aplicação
routes:
  - source: /
    destination: backend
  - source: /<path:rest>
    destination: backend